/**
 * A scene is used to render objects onscreen
 * 
 * @constructor
 * @type Scene
 * @see Canvas.startScene(scene)
 */
function Scene() {
  /**
   * This method is called when the scene is
   * created.
   */
  this.create = function() {
    
  };
  
  /**
   * This method is called when the screen
   * requests a draw.
   */
  this.draw = function() {
    
  };
  
  /**
   * This method is called every time the scene
   * is updated.
   */
  this.update = function() {
    
  };
  
  /**
   * This method is the last called on the scene.
   * Generally is not used.
   */
  this.destroy = function() {
    
  }
}